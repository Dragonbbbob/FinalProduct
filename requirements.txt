# Core dependencies
inference-gpu[yolo-world]==0.9.12rc1
supervision==0.19.0rc3
gTTS
opencv-python-headless  # Use the headless version for environments without a GUI
tqdm
ipython  # For IPython.display functionality

# Additional dependencies for Python-based web environments
numpy  # Required by OpenCV and YOLO models
requests  # Might be needed for inference library internals
